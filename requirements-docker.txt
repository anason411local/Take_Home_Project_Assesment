# =============================================================================
# Requirements for Docker (CPU-only, optimized for smaller image)
# =============================================================================
# This file contains only the essential dependencies needed to run the
# Sales Forecasting application in a Docker container.
# 
# CUDA/GPU packages are excluded for a smaller image size.
# =============================================================================

# Core Data Science
numpy==2.2.6
pandas>=2.0.0
scipy>=1.10.0
scikit-learn>=1.3.0

# Machine Learning Models
xgboost>=2.0.0
prophet>=1.1.0
statsmodels>=0.14.0

# MLOps
mlflow>=2.0.0
optuna>=3.0.0
optuna-dashboard>=0.14.0

# Web Framework
fastapi>=0.100.0
uvicorn>=0.23.0
pydantic>=2.0.0
python-multipart>=0.0.6
websockets>=11.0.0

# Database
SQLAlchemy>=2.0.0

# Visualization & Reporting
matplotlib>=3.7.0
seaborn>=0.12.0
plotly>=5.15.0

# Utilities
python-dotenv>=1.0.0
PyYAML>=6.0.0
tqdm>=4.65.0
joblib>=1.3.0

# SHAP for model explanations (optional, can be large)
# shap>=0.42.0

# HTTP client
requests>=2.31.0

